{
  "name": "soundscape",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter editor dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "preview": "pnpm --filter editor preview",
    "prepare": "husky",
    "test": "pnpm -r test",
    "test:run": "pnpm -r test run",
    "test:ui": "pnpm --filter editor test:ui",
    "test:coverage": "pnpm --filter editor test:coverage"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.0.1",
    "@commitlint/config-conventional": "^21.0.1",
    "husky": "^9.1.7"
  }
}
